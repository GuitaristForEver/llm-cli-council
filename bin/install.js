#!/usr/bin/env node
'use strict';

/**
 * llm-cli-council installer
 *
 * Installs the llm-cli-council Claude Code plugin to ~/.claude/skills/
 *
 * Full installer logic is implemented in Phase 2.
 * This file serves as the npm bin entry point scaffold.
 */

const path = require('path');
const os = require('os');

// Placeholder — Phase 2 implements full installation logic
console.log('llm-cli-council installer');
console.log('Full installer coming in next release.');
console.log('');
console.log('For now, install manually:');
console.log('  cp -r skills/* ~/.claude/skills/');
console.log('  cp -r lib ~/.claude/lib/');
console.log('  cp -r prompts ~/.claude/prompts/');
console.log('  cp -r rules ~/.claude/rules/');
console.log('  cp config/providers.json ~/.claude/config/');
